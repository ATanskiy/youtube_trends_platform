version: 2

sources:
  - name: bronze
    schema: bronze    # Just use the full path here
    tables:
      - name: users_raw
        description: 'Raw JSON events for users ingestion'
        columns:
          - name: event_id
            tests:
              - not_null
              - unique
          - name: json_raw
          - name: inserted_at